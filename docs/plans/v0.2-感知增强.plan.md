# Plan（过程重建）：v0.2 感知增强与可观测

> **类型**：过程重建  
> **日期**：2026-06-19  
> **用户原话**：「今天再完善下」  
> **版本**：v0.2.0

---

## 目标

在 v0.1 全链路基础上，提升 **去重精度、无 VLM 可用性、可观测性**。

## 任务清单

### 1. L2 直方图去重

- 在 pHash 之后增加 RGB 直方图余弦相似度
- Pipeline 统计 `dedup_skipped` / 命中率
- **Commit**：`8d9f14c`

### 2. Windows 前台窗口感知

- 新建 `capture/context.py`：读取活动窗口标题、进程名
- 启发式 `vlm.py` 利用前台上下文推断 page_category / user_action
- 无 VLM API 也能给出合理理解
- **Commit**：`9865a6b`

### 3. Activity 聚合优化

- 同场景连续帧合并，`frame_count` 字段
- 日报表格化、时间条展示
- **Commit**：`14b6ec9`

### 4. CLI 可观测

- 新增 `timeline`、`stats` 子命令
- **Commit**：`4a67303`

### 5. 测试与文档

- `tests/test_core.py` 去重与聚合测试
- 更新 README、architecture
- **Commit**：`f39b007`

## 验收

```powershell
python main.py once
python main.py timeline
python main.py stats
python -m unittest discover -s tests
```

## 完成后建议（会话中提出）

下一步可选：真实 VLM / embedding / Web UI → 见 [v0.3](v0.3-VLM-Embedding-WebUI.plan.md)
